cluster.name: "elk-cluster"
network.host: 0.0.0.0

# Enable Elastic Security (X-Pack Authentication)
xpack.security.enabled: true
xpack.security.http.ssl.enabled: false # Keep false for basic internal dev, enable HTTPS for production


